// TODO: Componente de formulario de login

interface LoginFormProps {
  // Props del componente
}

export const LoginForm = (props: LoginFormProps) => {
  return (
    <div>
      {/* TODO: Implementar formulario de login */}
    </div>
  );
};
